// Stable provider compatibility facade. The implementation remains in one
// cohesive owner until each capability has enough independent state and
// dependencies to justify a real module boundary.
export * from "./provider-selection.js";
